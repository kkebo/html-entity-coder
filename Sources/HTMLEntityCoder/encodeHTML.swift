/// Replaces any characters that aren't printable ASCII characters and `&`, `<`, `>`, `"`, and `'` with HTML character references.
public func endodeHTML(_ input: String) -> String {
    fatalError("not implemented")
}
